import { DisorderListPipe } from './disorder-list.pipe';

describe('DisorderListPipe', () => {
  it('create an instance', () => {
    const pipe = new DisorderListPipe();
    expect(pipe).toBeTruthy();
  });
});
